[package]
name = "core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = {version="1.0", features=["derive"]}
typed-builder = "0.23.2"
ordered-float = "5.3.0"
stochastic = {path = "../stochastic"}
error = {path="../error"}
