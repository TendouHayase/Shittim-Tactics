[package]
name = "bosses"
version = "0.1.0"
edition = "2024"

[dependencies]
core = {path = "../core"}
parsing-json={path="../parsing-json"}
error = {path="../error"}
serde = {version="1.0", features=["derive"]}
typed-builder = "0.23.2"