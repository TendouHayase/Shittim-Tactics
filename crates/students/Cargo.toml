[package]
name = "students"
license.workspace = true
version.workspace = true
edition.workspace = true

[dependencies]
core = {path = "../core"}
macros = {path = "../macros"}
serde = {version = "1.0", features = ["derive"]}